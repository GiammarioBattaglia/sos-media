# HEF Focus — Trump AI

Autonomous deployment source for the dedicated Vercel project `hef-focus-trump-ai`.

The build reconstructs the reviewed static package and publishes:
`/article/trump-prega-ia/`

The HTML contains canonical, Open Graph and Twitter metadata in the initial server response.
